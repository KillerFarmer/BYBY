'use strict';

describe('myApp.verify module', function() {

    beforeEach(module('myApp.verify'));

    describe('verify controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var signUpCtrl = $controller('VerifyCtrl');
            expect(signUpCtrl).toBeDefined();
        }));

    });
});