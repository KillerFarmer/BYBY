'use strict';

describe('myApp.registerRecipe module', function() {

    beforeEach(module('myApp.registerRecipe'));

    describe('registerRecipe controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var view3Ctrl = $controller('RegisterRecipeCtrl');
            expect(view3Ctrl).toBeDefined();
        }));

    });
});