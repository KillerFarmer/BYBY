'use strict';

describe('myApp.batchView module', function() {

    beforeEach(module('myApp.BatchViewCtrl'));

    describe('BatchView controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var view3Ctrl = $controller('BatchViewCtrl');
            expect(view3Ctrl).toBeDefined();
        }));

    });
});