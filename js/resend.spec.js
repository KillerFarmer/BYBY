'use strict';

describe('myApp.resend module', function() {

    beforeEach(module('myApp.resend'));

    describe('resend controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var resendCtrl = $controller('ResendCtrl');
            expect(resendCtrl).toBeDefined();
        }));

    });
});